# 배포 파이프라인 `RH*`(하이브리드 계열) 선정 근거 — 논문·중간보고서용

> 코드 인용은 전부 **원문 확인**(2026-08-31)이고 줄 번호는 `third_party/FoundationPose` 기준이다.
> 측정치의 정본은 **`RESULTS.md`** 이고 여기서는 §번호로 가리킨다. **수치를 여기서 새로 만들지 않는다.**

---

## 0. 🔴 주장의 정확한 범위 — 먼저 못 박는다

> 🟢 **중간 보고서 단계의 서술 방침**(사용자 확정 2026-09-01) — 본문은 **`RH*` 계열 기반**으로 쓰고,
> **`RH1` 을 «잠정 최종»** 으로 명시한다. 프롬프트도 같은 방식으로 **`f002` 를 «잠정 최종»** 으로 쓴다.
> 🔴 «최종 확정» 이 아니라 «잠정» 인 이유가 §0·§4 이고, **그 유보를 본문에 같이 적는다.**

**주장할 수 있는 것**

> **하이브리드 계열 `RH*`(회전은 stage1, 평행이동은 stage2)가 어느 단일 단계보다 낫다.**

**주장하면 안 되는 것**

> ~~`RH1` 이 최고 성능이다~~ — `RH1`↔`RH2` 는 `--input-scale` 만 다른 **같은 방법의 두 구현**이고
> **통계적으로 구분되지 않는다**(§43-2: R·t·ADD 부호검정 **p ≥ 0.30**, `|t 차|` 중앙 0.065mm =
> FP 재실행 잡음 바닥의 26%). 마찬가지로 `RP1`/`RP2`/`RP3` 도 서로 구분되지 않는다(§38-9, **p = 1.000**).

★ **이 «구분 안 됨» 은 약점이 아니라 논거다.** 방법의 이득이 **구현 세부(입력 배율·투영 방식)에
민감하지 않다**는 뜻이고, 그것이 «하이브리드» 라는 **방법 수준의 주장**을 오히려 강하게 만든다.
→ 논문에서는 **`RH*` 계열 대 `RP*` 계열**로 쓰고, 계열 안의 선택은 **비용·안정성**으로 서술한다.

---

## 1. 파이프라인은 결정 6개다 — 확정된 넷, 구분 안 되는 둘

```
stereo(ONNX) → SAM3 텍스트 full → pose_fp --primary full --input-scale s (stage2 on)
             → 하이브리드 R = pose_coarse · t = pose_refined      (정합 없음)
```

| # | 결정 | 채택 | 근거의 성격 | 확정? |
|---|---|---|---|---|
| **D1** | 분할: exemplar ↔ **텍스트** | 텍스트 | 실물에서 exemplar **전멸**(§38-1) | ✅ 확정 |
| **D2** | `--primary`: **full** ↔ flange | full | flange 는 t 가 1.8배 좋지만 **R 이 2배 나쁘고**(§37-9b) 마스크가 조금만 어긋나면 **90°/180° 뒤집힌다**(§32-1) · 프롬프트 여유 2/20(§41-2) | ✅ 조건부 확정 |
| **D3** | stage2: **on** ↔ off | on | 하이브리드가 성립하려면 `pose_refined` 가 있어야 한다. 실물 통과 체인이 on 이었다(§38) | ✅ 확정 |
| **D4** | **R·t 출처: 하이브리드** ↔ coarse ↔ refined | 하이브리드 | **아래 §2 — 이 문서의 본론** | ✅ **가장 강한 근거** |
| **D5** | 테두리 정합: on ↔ **off** | off | 실물 통과 체인에 없다(§38) · 검정 몸체에서 **계통 편향 −2.04px**(§35-2i) · 50cm 검정에서 KPI 20→11(§35-2m-6) | ⚠️ 조건부 |
| **D6** | `--input-scale`: 0.75 / 0.5 / hull | 0.75(=RH1) | 🔴 **구분되지 않는다**(§38-9 p=1.000 · §43-2 p≥0.30). **§38 에서 실물로 검증된 값**이라 골랐다 | ❌ **미확정** |

⚠️ **D6 은 정확도 근거가 없다.** 논문에서는 *"구현 세부이며 측정 한계 안에서 동등하다"* 로 쓰고,
실용 지침은 **비용 1,255ms ↔ 1,123ms · `0.75` 는 `PYTORCH_CUDA_ALLOC_CONF=expandable_segments` 필요**
(§38-4)로 서술한다.

---

## 2. 본론 — 왜 하이브리드가 성립하고, 왜 이득이 나는가

### 2.1 «접합이 잘 정의된다» — 자세 갱신이 **egocentric 이라 R·t 가 분리된다** (코드)

`Utils.py:850-857`

```python
def egocentric_delta_pose_to_pose(A_in_cam, trans_delta, rot_mat_delta):
    B_in_cam[:, :3, 3]  = A_in_cam[:, :3, 3] + trans_delta        # t ← t + Δt
    B_in_cam[:, :3, :3] = rot_mat_delta @ A_in_cam[:, :3, :3]     # R ← ΔR · R
```

- 평행이동은 **카메라 좌표에서 덧셈**이고 `R` 이 안 끼어든다.
- 회전은 **좌곱**이고 `t` 가 안 끼어든다.

★ 그래서 **한 단계의 `R` 과 다른 단계의 `t` 를 접합해도 각각이 그 단계에서 최적화된 그대로 유지된다.**
🔴 물체 좌표계(`t ← R·Δt + t`) 갱신이었다면 접합이 의미를 잃는다 — **이 파라미터화가 전제 조건**이다.

### 2.2 «왜 두 단계가 서로 다른 자유도에서 좋은가» — **stage2 는 메쉬를 갈아탄다** (코드)

`pose_fp.py:286-293` — `est1 = FoundationPose(mesh=mesh_primary)` · `est2 = FoundationPose(mesh=mesh_flange)`.
즉 **stage2 = «한 번 더 refine» 이 아니라 «`full` → `top_flange` 로 메쉬를 바꿔 다시 추정»** 이다.
메쉬 교체가 두 가지를 **비대칭으로** 바꾼다:

**(a) 유효 해상도 — 평행이동에 유리하게** (`Utils.py:605`, `config.yml`)

```python
radius = mesh_diameter * crop_ratio / 2        # crop 은 3D 에서 정의된다
```
`input_resize = 160` · `crop_ratio = 1.2`(refiner) / `1.1`(scorer).
→ **네트워크 1px = `diameter × crop_ratio / 160` mm 이고 거리·`fx` 와 무관하다.**
배포 자산 `foup_300_semi_r2` 실측:

| 메쉬 | diameter | refiner 1px | scorer 1px |
|---|--:|--:|--:|
| `full.ply` | 560.6mm | **4.204 mm** | 3.854 mm |
| `top_flange.ply` | 183.1mm | **1.373 mm** | 1.259 mm |

**stage2 에서 유효 해상도가 3.06배 좋아진다.**

**(b) 갱신 보폭 — 평행이동만 줄고 회전은 그대로** (`predict_pose_refine.py:221-229`)

```python
rot_mat_delta = torch.tanh(output["rot"]) * self.cfg['rot_normalizer']   # 0.3490658… rad = ±20°
...
trans_delta  *= (mesh_diameter / 2)                                       # 메쉬 크기에 비례
```
🔴 **평행이동 보폭은 `mesh_diameter` 에 비례하고 회전 보폭은 상수(±20°)다.**
메쉬가 3.06배 작아지면 **평행이동 보폭만 3.06배 세밀해지고 회전 보폭은 안 바뀐다.**

**(c) 회전 증거는 오히려 줄어든다**

`top_flange` 는 근사 4회 대칭이고 방향 정보가 **표면의 3.5%·전부 경계**에 있다(§ flange 의 회전 구속).
stage2 는 **몸체가 주던 회전 증거를 버린다.**

→ ★★ **stage2 는 «회전 증거» 를 «평행이동 해상도» 와 맞바꾼다.**
그래서 `coarse` 는 R 이 좋고 t 가 나쁘며 `refined` 는 정확히 반대다.

### 2.3 그 맞바꿈이 **세 파이프라인에서 동시에** 관측된다 (측정)

| 출처 | coarse R / t | refined R / t |
|---|---|---|
| 원거리 `full` (§27-7, n=120) | **0.549°** / 1.713mm | 0.737° / **1.280mm** |
| 근접 `flange` (§27-7, n=120) | **0.510°** / **0.928mm** | 0.656° / 1.104mm |
| 실물 검증 체인 3종 (§38-7, n=10) | **0.37~0.50°** / 2.0~2.3mm | 1.36~1.42° / **1.04~1.26mm** |

**부호가 한 번도 뒤집히지 않는다.** §2.2 의 기전이 예측하는 그대로다.

### 2.4 그래서 하이브리드가 최선이다 (측정)

§38-7 (sim GT, 10프레임): **하이브리드 ADD 중앙 1.395mm** — 어느 단일 단계보다 **1.5~2배** 좋고,
`refined` 단독은 KPI **9/10** 인데 하이브리드는 **10/10**.
§42(sim, 120프레임 · 오선택 제외 93프레임): `RH1` **KPI 100% · R 중앙 0.643° · t 중앙 0.773mm**.

**레버암** — 회전 오차가 ADD 에 실리는 배율(배포 자산 실측, 원점 = flange 상면 중심):

| 메쉬 | 정점 |v| 평균 | 레버암 |
|---|--:|--:|
| `full.ply` | 93.9mm | **1.639 mm/도** |
| `top_flange.ply` | 50.6mm | 0.883 mm/도 |

→ `full` 기준 **R 0.1° 가 ADD 0.16mm 에 해당**한다. §2.3 표에서 refined 가 R 을 0.19~0.92° 잃는데,
그것이 ADD 로 **0.31~1.51mm** 다 — refined 가 t 에서 버는 0.43mm 를 **넘는다.**
★ **이것이 «refined 단독이 아니라 하이브리드» 인 정량적 이유**다.

### 2.5 ★★★ 반증 가능한 예측 — **세우고, 시험했고, 맞았다**

§2.2 가 옳다면 다음이 따라온다:

> **`--primary flange` 로 돌리면 stage1·stage2 가 «같은 메쉬」라 메쉬 교체가 없고,
> 따라서 stage2 의 이득도 하이브리드의 이득도 사라져야 한다.**

측정(교훈 #95): `--primary flange` 에서 stage2 의 t 이득이 **1.100 → 1.042mm (0.058mm)** 인데
**FP 재실행 잡음 바닥이 0.512mm** 다 — **측정되지 않는다.** 오히려 `refined` 단독은 R 최대가
1.24 → 3.68° 로 터진다.

★ **이 예측-검증 쌍이 논문에서 가장 강한 부분**이다. «단계를 하나 더 두면 좋다» 가 아니라
**«그 단계가 메쉬를 갈아타기 때문에 좋다»** 를 구분해 보였다.

---

## 3. 나머지 결정의 근거 요약

### D1 텍스트 분할 — exemplar 를 왜 버렸나
sim 에서 만든 SAM3 exemplar 참조가 **실물에서 전부 실패**했다(§38-1). 참조는 **거리·외관 종속**이라
(원거리 참조로 근접 질의 시 IoU 0.044) 배포 조건마다 재생성해야 하는데, 실물 도메인에서 그 재생성
자체가 «실사진으로 참조를 만드는 것» 이라 프로젝트 제약과 충돌한다. 텍스트는 그 의존이 **원천적으로 없다.**

### D2 `--primary full` — flange 가 더 정확한데 왜 안 쓰나
§2.2(a)대로 `flange` 는 유효 해상도가 3.06배 좋고 실제로 **t 1.973 → 1.095mm(1.8배)** 다(§37-9b).
그런데 대가가 셋이다 — **① R 중앙 ~1.0° vs `full` 0.45~0.52°(2배)** **② 마스크가 조금만 어긋나면
90°/180° 뒤집힌다**(IoU ≥0.98 조건, §32-1) **③ 그 마스크를 낼 프롬프트가 20개 중 2개뿐**(§41-2).
🔴 **회전 뒤집힘은 t 이득과 맞바꿀 수 있는 종류의 오차가 아니다.**
★ 하이브리드는 **`full` 을 주 메쉬로 두면서 stage2 를 통해 flange 의 해상도 이득만 t 로 받는다** —
D2 와 D4 가 같은 문제의 두 답이고, 하이브리드가 그 절충이다.

### D5 정합 없음
§23·§26 에서 sim 이득이 컸으나(단일 시점 R 0.618→0.216°) **실물 통과 체인에는 없었다**(§38).
그리고 검정 몸체에서 **계통 편향**이 확인됐다 — GT 를 초기값으로 줘도 **−2.04px 로 일관되게** 움직이고
(§35-2i), 프레임마다 같은 방향이라 **게이트가 못 막는다**. 50cm 검정에서 KPI **20 → 11**(§35-2m-6).
⚠️ 주황·투명 몸체에서는 오히려 R 이 1.5~2.0배 좋아진다 — **몸체 대비에 딸린 조건부 결정**이고,
런타임 판정값은 **«정합 이동량 t 중앙 ≥10mm 면 끈다»** 다.

---

## 4. 🔴 한계 — 논문에 반드시 같이 쓸 것

1. **«최고 성능» 주장 불가.** 계열 안(RH1↔RH2, RP1↔RP2↔RP3)은 구분되지 않는다.
   비교는 **계열 수준**(`RH*` vs `RP*`)에서만 유효하다.
2. **실물에 GT 가 없다.** §2.3·§2.4 의 R/t 수치는 **전부 sim GT** 다. 실물 근거는 **§38 의 육안 확인**
   하나이며 «눈으로 오차가 분간 안 됨» 이 그 강도다. 절대 정확도는 **상대 GT**(§7.5c, `tools/relative_gt.py`)
   로만 잴 수 있고 **아직 안 쟀다.**
3. **CAD-실물 불일치 축이 열려 있다** — sim 은 렌더와 CAD 가 같은 메쉬라 불일치가 0 이다(§20·§29).
4. 🔴🔴 **KPI 를 실제로 지배하는 것은 이 결정들이 아닐 수 있다.** 방해물 씬에서 **분할 오선택이
   19~29%** 였고, 그때 `RH1`·`RH2` 는 **프레임 단위로 똑같이 실패**했다(§42-8: COMBO 팔이 마스크를 공유).
   **하이브리드 선택은 그 축을 못 고친다** — 처방은 사전 위치 가드(§34-10)다.
   ⚠️ 단일 물체 씬에서는 이 축이 안 열린다.
5. **표본** — §38-7 은 n=10, §42 는 n=120(오선택 제외 93). n=40 무결점의 실패율 95% 상한이 7.5% 다(교훈 #58).

---

## 5. 프롬프트 `f002` 선정 — **설계에서 유도하고, 실험으로 확인했다**

🔴 **먼저 범위**: 측정은 *"f002 가 최고"* 를 말하지 **않는다.** §2 의 `RH*` 와 **같은 구조**다 —
**계열은 갈리고 계열 안은 안 갈린다.** 실물 3라운드에서 **갈린 이미지가 0장**이고(§39-17)
sim 방해물 씬에서 f002↔f005↔f007 이 **McNemar p = 0.078~0.289**(§42-6 부속)다.
→ 주장은 **«이런 구조의 명사구를 쓴다»** 이고, `f002` 는 **그 구조의 대표**다.

### 5.1 SAM3 는 «짧은 명사구(NP)» 를 개념 프롬프트로 받도록 설계됐다 (원문)

| 근거 | 원문 |
|---|---|
| **입력 단위** | `README:49` — *"exhaustively segment all instances of an open-vocabulary concept specified by a **short text phrase** or exemplars"* |
| **학습·평가 단위** | `README:351` — SA-Co 는 *"images … with annotated **noun phrases**. Each image/video and **noun phrase** pair is annotated with instance masks"* (270K concepts) |
| **복잡한 프롬프트는 분해 대상** | `sam3/agent/agent_core.py:238·273·423` — 실패 시 재시도 지시가 *"a different, perhaps more general, or more creative **simple noun phrase** prompt"* 다. 🔴 **Agent 가 존재하는 이유 자체가 «SAM3 본체는 단순 NP 를 받는다»** 는 것이다 |
| **수식어 구분 기전** | `README:49` + `sam3/model/decoder.py:104-137` — **presence token** 이 *"closely related text prompts (e.g., 'a player in white' vs 'a player in red')"* 를 구분하도록 도입됐다 |

### 5.2 우리 경험 규칙이 **그 설계의 따름정리**다

| 우리 규칙 (측정) | 설계로부터의 설명 |
|---|---|
| **① 약어 단독은 죽는다** — `FOUP` 0/9 ↔ `front opening unified pod` 9/9 (§37-13) | SA-Co 는 **NP 표층형**으로 라벨링됐다. 약어는 그 분포에 거의 없고 풀어 쓴 형태가 있다 |
| **③④ 형상 접지는 «수식어 자리» 에서만 작동** — `cube shaped case` 9/9 ↔ **`plastic cube` 0/9** (§37-15) | NP = `[modifier*] + HEAD`. `plastic cube` 는 **head 가 `cube`**(우리 물체가 아니다). `cube shaped case` 는 head 가 `case`, `cube shaped` 가 modifier — **presence token 이 구분하도록 설계된 그 구조**다 |
| **⑦ 핵명사를 빼면 «검출 자체» 가 깨진다** — 9/9 → 7/9 (§37-15-1) | **head 가 개념을 정하고 modifier 는 좁힐 뿐**이다. head 가 없으면 개념이 없다 |
| **⑥ 슬롯별 최적을 조합하면 최적이 안 나온다** (§37-15-3) | SAM3 는 phrase 를 **통째로 임베딩**한다. 단어별 기여의 합이 아니므로 **슬롯 독립성이 성립하지 않는다** |
| **⑩ 색어는 «조건부» 다 — 맞는 색에서만 걸리고 아니면 조용히 검출 0** (§37-16-2) | 🔴 **버그가 아니라 설계된 동작**이다. presence token 이 색 수식어로 구분하도록 만들어졌다 |
| **⑨ 영어 전용** — 한/일/독 전부 0/9 (§37-13) | SA-Co 가 **영어 NP** 데이터셋이다 |
| **⑧ 대소문자는 «완전히 같다» · 마침표는 마스크를 바꾼다** (§39-13d) | 토크나이저 정규화가 대소문자를 지운다. 마침표는 **토큰을 추가**해 임베딩을 바꾼다 |
| **⑭ 제조사명은 해롭다** — 그 제조사 사진에만 붙는다 (§39-5) | 270K 개념이라도 **상표는 특정 외관과 강하게 결합**한 spurious correlation 이다 |

### 5.3 그래서 `f002` 는 **설계 요건의 교집합**이다

`cube shaped sealed plastic wafer pod`

| 요건 (§5.1) | 충족 |
|---|---|
| **짧은 명사구** (문장 아님) | 6단어 NP ✅ |
| **도메인 head noun** | `pod`, `wafer` 로 한정 ✅ |
| **형상 접지가 수식어 자리** | `cube shaped` ✅ (규칙 ③④) |
| 재질·상태 수식어 | `sealed` · `plastic` ✅ |
| **약어 없음 · 색어 없음 · 마침표 없음 · 제조사명 없음** | ✅ (규칙 ①⑧⑩⑭) |

### 5.4 실험적 확인 — **범위를 넓혀 가며 세 표본에서**

| 표본 | 규모 | 결과 |
|---|---|---|
| 웹 FOUP 사진 | **237장 × 136개**, 사람 라벨 79장 | `cube shaped …` 계열이 사람 판정 **상위 13개 중 9개**(§39-5) |
| 실물 ZED X | 3라운드 (136 → 81 → 70 → 58) | 통과 집합이 **«중첩» 으로만** 줄었다(신규 0) — 좁히기가 성립 |
| sim GT | 120프레임 × 4 프롬프트 | 오선택 제외 **KPI 99~100% · t 중앙 0.774~0.810mm** (§42) |

곁들여 **대조군 설계**가 들어 있다 — `f019` `a boxy plastic object`(**도메인어 0**)를 매 라운드
상설로 돌려 *"도메인 어휘가 실제로 필요한가"* 를 조건마다 다시 잰다. sim 방해물 씬에서
**f019 가 오선택률 꼴찌(28.8%)** 로 나왔다.

### 5.4b ★★ 실물 `score` 여유 — **f007 은 갈리고, f002↔f005 는 안 갈린다** (§43-7)

| | sim 오선택률 (n=118) | 실물 `score` 여유 |
|---|---|---|
| **f002** `cube shaped sealed plastic wafer pod` | 22.9% | **근소 우세** |
| **f005** `plastic cube shaped sealed wafer pod` | **19.5%** | 근소 열세 |
| **f007** `boxy sealed plastic wafer pod` | **27.1%** | 🔴 **확실히 열세** |

- ★ **`f007` 만 두 표본에서 같은 방향으로 뒤진다** — **방향이 일치하는 첫 프롬프트 신호**다.
- 🔴 **`f002`↔`f005` 는 방향이 엇갈리고(sim 은 f005, 실물은 f002) 둘 다 근소**하다 →
  어순 변형이 표본마다 뒤집히는 것은 **잡음의 모습**이다. §5.5 의 결론이 유지된다.

🔴 **`score` 를 «품질» 로 읽지 않는다** — 마스크 품질과 **r = +0.06**(교훈 #90)이고,
136개 전체로는 +0.616 인데 반쪽 안에서는 +0.32~0.38 로 사라진다(교훈 #102):
★ **«가르는» 선별기로는 유효하고 «줄 세우는» 서열기로는 무효**다.
그런데 **여기서는 선별기 용도가 맞다** — §37-5 가 *"프롬프트가 바꾸는 것은 검출률뿐인데 그게 곧 KPI"*
라고 쟀으므로 **`score` 여유(미검출까지의 마진)는 KPI 로 직결**된다.
→ 읽는 법: *"f007 의 마스크가 나쁘다"* 가 아니라 **"f007 은 조건이 나빠지면 먼저 사라진다"**.

### 5.5 ★★ 이론의 입도와 측정의 입도가 **일치한다** — 이것이 결론이다

- **이론은 «구조» 를 예측하지 «어순» 을 예측하지 않는다.** NP 설계는
  `[형상어][상태][재질] + [도메인 head]` 를 지시할 뿐, `cube shaped sealed plastic wafer pod` 와
  `plastic cube shaped sealed wafer pod`(f005) 중 어느 쪽인지는 말하지 않는다.
- **측정도 그 둘을 못 가른다** (p = 0.078~0.289 · 실물에서 갈린 이미지 0장).

★★★ **모순이 아니라 같은 말이다.** 논문에서는 *"f002 가 이겼다"* 가 아니라
**"설계 요건을 만족하는 명사구 집합이 이기고, 그 집합 안에서는 구분되지 않는다"** 로 쓴다.

★★ **그리고 §5.4b 가 그 구조를 «측정으로» 재현한다** — 구조가 다른 `f007`(형상어가 `boxy`)은
**두 표본에서 일관되게 뒤지고**, 구조가 같은 `f002`↔`f005`(어순만 다름)는 **방향이 엇갈린다.**
🔴 **이론이 «구조» 를 예측했고 측정이 정확히 «구조 경계에서만» 갈렸다.** 이보다 좋은 확인은 없다.

→ `f002` 는 **그 집합의 대표**이자 **두 표본 «둘 중 나쁜 쪽» 기준의 상위**(§39-24)여서 골랐다.

### 5.6 🔴 한계

1. **«구분 안 됨» 은 «같음» 이 아니다**(교훈 #86). n=118~237 로도 못 가른 것이지 동일함을 보인 게 아니다.
2. **오선택 축은 프롬프트로 안 닫힌다** — sim 방해물 씬에서 프롬프트 간 차이(19~29%)가 있었으나
   **McNemar 로 유의하지 않았고**, 그 오선택의 실제 원인은 **선택 규칙의 점수 게이트**였다(§42-6·§42-9).
3. **`flange` 프롬프트는 이 이야기가 안 통한다** — 웹 237장 중 **234장이 갈리고**(`full` 은 79장),
   실물에서 20개 중 **2개만** 살아남았다(§41). **`full` 은 도메인을 넘고 `flange` 는 안 넘는다.**

---

## 6. 🔴 구성요소 근거 감사 — **출처를 갈라서**

> 사용자 지적(2026-09-01): *"코드 기반 설명을 할 때는 우리가 구현한 것과 third_party 를 구분해 달라.
> 가능하면 **논문과 third_party 에서** 근거를 찾고 싶다."* — 옳다.
> 🔴 **«우리 코드가 그렇게 돼 있다» 는 근거가 아니라 «구현» 이다.**

| 표기 | 출처 | 논문에서의 강도 |
|:-:|---|---|
| **P** | 논문·모델 README (upstream **주장**) | 강함 |
| **T** | `third_party/` 코드 (upstream **구현**) | 강함 |
| **S** | 외부 규격 문서 (SEMI E47.1) | 강함 |
| **O** | **우리 코드** (`spatial_vision/`) | 🔴 **근거가 아니라 «결정의 서술»** |
| **M** | 우리 측정 (`RESULTS.md`) | 조건부(sim/실물·표본) |

### 6.1 ★ 이 프로젝트의 논증 구조 — **결정은 우리 것, 기전은 upstream 것**

이 표를 만들며 드러난 것: **파이프라인의 «결정» 은 전부 O 이고(당연하다, 그게 우리 기여다),
그 결정이 «왜 통하는가» 는 T/P 가 준다.** 논문에서는 이 분리를 명시하는 것이 정확하다 —

> *"우리는 X 를 선택했다(기여). 그것이 통하는 이유는 upstream 모델의 Y 라는 성질이다(근거).
> 그리고 Z 로 측정했다(검증)."*

🔴 **«기전이 T 인데 결정이 O» 인 것은 약점이 아니다.** 약점은 **기전조차 O 인 것**이다 — 아래 #4 가 그렇다.

### 6.2 감사표

| # | 구성요소 | 결정 | **기전 근거** | 측정 | 등급 |
|---|---|:-:|---|:-:|:-:|
| 1 | stereo = NGC ONNX | O | **L** 라이선스 문서 — GitHub 배포는 research-only, NGC 는 *"ready for commercial use"* | M | A ⚠️ **정확도로 고른 게 아니다** |
| 2 | 텍스트 분할 | O | **P** `sam3/README:49` *"short text phrase"* · `:351` SA-Co 는 **noun phrase** 라벨 · **T** `agent_core.py:238·273·423` 실패 시 *"simple noun phrase"* 로 재시도 | M §38-1 | **A** |
| 3 | 프롬프트 `f002` | O | **P/T** 위와 같음 + **T** `decoder.py:104-137` **presence token**(수식어 구분) | M 3표본 | **A** |
| 4 | `--select center` + `score_frac 0.9` | O | 🔴 **없다 — 기전이 O 다.** upstream 에 대응물이 없는 **우리 휴리스틱**이고, §6.4 가 **왜 `score` 로는 원리적으로 안 되는지**를 upstream 코드로 보인다 | ✅ **M §44-17 — 배포 조건에서 «무영향»**: 단일 물체 씬 **304케이스에서 `score`·`center0.9`·`center0.3` 이 0% 불일치**(마스크가 소수점까지 같다). 🔴 동종 방해물 씬에서만 15~36% 오선택(§44-15) | **D** ⚠️ **단 배포 조건에서는 결과에 영향을 주지 않는다** |
| 5 | `--text-conf 0.05` | O | **T** `sam3_image_processor.py:199` `keep = out_probs > confidence_threshold` (절대 문턱). 🔴 **값의 근거는 없다** | M §35-2m-2 | **B** |
| 6 | `--primary full` | O | **T** `Utils.py:605` crop = `diameter × crop_ratio`(3D) → 유효 해상도 | M §37-9b | **A** |
| 7 | `--input-scale 0.75` | O | **T** `estimater.py:173-174` `erode_depth(radius=2)`·`bilateral_filter_depth` 가 **픽셀 단위** → 배율이 전처리 반경을 바꾼다 | 🔴 **구분 안 됨** | **B** |
| 8 | stage2(메쉬 교체) | **O** | **T** `Utils.py:605`(crop) · `predict_pose_refine.py:229`(`trans_delta *= mesh_diameter/2`) · `:221`(`rot_normalizer` **상수**) | M §27-7·§38-7 | **A** |
| 9 | ★ 하이브리드 R·t 접합 | **O** | **T** `Utils.py:850-857` `t ← t+Δt` · `R ← ΔR·R` — **egocentric 이라 분리된다** | M §38-7 · **§2.5 예측 검증** | **A+** |
| 10 | 정합 없음 | O | — (경험) | M §35-2i·§38 | **B** |
| 11 | CAD `foup_300_semi_r2` | O | **S** SEMI E47.1-1101 원문(`z49`·`d63` 정의) | M §31·§36 실측 | **A** |
| 12 | pose 원점 = flange 상면 | **O** | **T** `estimater.py:137-150` `guess_translation` 이 `zc = median(depth[mask])` — **원점이 관측 표면 근처여야** 초기값이 맞는다 | M `verify_obj` | **A** |
| 13 | 카메라 ZED X 2.2mm | O | 기본 기하(`σ_Z ∝ Z²/(fx·B)`) | M §34 | **A** |
| 14 | ★ `--est-iter 5` | — | **T** `run_demo.py:20` `est_refine_iter` **기본 5** + `estimater.py:159` `def register(…, iteration=5)` — **저자 기본값 두 곳**. RH1 의 stage1 은 init 이 없어 `register` 경로(`pose_fp.py:374`)라 **정확히 그 경로**다 | — | **A** ★ *"저자 기본값을 그대로 썼다"* 로 쓸 수 있다 |
| 15 | `--refine-iter 5` | **O** | **upstream 과 어긋난다** — stage2 는 `est2.track_one(…)`(`pose_fp.py:416`)인데 `run_demo.py:21` 의 `track_refine_iter` 기본은 **2** 다 | ✅ **M §44-12 — 2·5·10 이 구분 안 됨**(차이가 재실행 잡음 바닥 아래) | **A−** ★ *"어긋남을 확인했고 무해함을 측정했다"* |
| 16 | `--stereo-scale 0.5` | O | ✅ **T/M — 제약과 비용이 정한다.** 1920×1200 에서 **0.625·0.75 가 CUDA EP 로 실행 불가**(31GB 유휴 GPU 에서 재현 · ORT 세션 옵션 5조합 전부 실패 — `arena_extend_strategy` 는 Softmax OOM 을 넘기지만 ConvTranspose cuDNN 워크스페이스에서 다시 막힌다), 상한은 **0.5625**. ⚠️ `PYTORCH_CUDA_ALLOC_CONF` 는 **다른 프레임워크**라 무관하다(그건 `--input-scale` 용). 남은 여유의 값어치를 **정량화했다** — 0.5→0.5625 는 t **0.147mm**(KPI 의 2.9%) 이득에 **+36% 비용**(예산의 5.4%)이고 **회전은 불변**(p=1.000) | M §44-13 (n=60·3반복) | **A** ⚠️ 정확도 최적은 0.5625 다 |
| 17 | `--flange-mask-proj faces` | O | **M** 교훈 #20 — `convexHull` 은 오목 노치를 메워 GT 대비 **평균 1.55% 부푼다**. 기전 근거는 없고 **측정으로 선다** | M | **B** |
| 18 | `--flange-mask-from pose` | — | 기본값. **T** stage2 의 flange 마스크를 coarse pose 투영으로 만든다 = SAM3 flange 의존을 없앤다 | M §38-12 동등 | **A** ⚠️ 정확도 근거 아님(의존성 축소) |
| 19 | ★ **배포 거리 «≥56cm»** (하한만) | **O** | 🔴🔴 **기전이 «가설» 로 내려갔다**(§44-24d, 2026-09-02) — ~~`estimater.py:142-150` `guess_translation` = bbox 중심 → 잘리면 초기값이 수렴 분지 경계~~ 는 **sim 이 재현하지 못했다**: 씬 고정 사다리 0.291m 에서 **85% 가 잘리는데** 「잘렸는데 마스크는 맞은」 50장이 **ADD 중앙 1.135mm · 실패 0** 이다. 잘림이 때린 것은 pose 가 아니라 **분할**이었다(오선택 18/68 vs 안 잘린 0/12). · **T** `Utils.py:605` crop 이 3D 기준이라 **t 천장은 거리 무관**(이 항은 유효) | ✅ **M §44-2 실물 n=40×3거리** — 28cm `full` **19.4% [14.0, 26.2] 대실패**(팔로 못 품) ↔ 56·66cm **0/40** | **B**(하한) ⚠️ **관측은 실물로 확고한데 기전이 없다** · 🔴 **«대역» 은 미정**(§44-18c) |
| 20 | ★★ **KPI `t ≤ 5mm · R ≤ 3°`** | **O** | 🔴 **유도가 기록돼 있지 않다** — `RESULTS.md §KPI` 는 *"2026-08-07 확정"* 이라고만 적혀 있고 `CONSUMER_6DPOSE.md`(요구사항 근거 문서)에도 공차 유도가 없다. ★ **사후 정합성은 세울 수 있다**(§8): 두 항이 **flange 좌표계에서만 균형이 맞는다**(R 3° 의 flange 표면 최대 변위 **4.81mm** ≈ t 예산 5mm. 같은 3° 가 `full` 표면에서는 **22.4mm** = 4.5배 불균형) | ✅ **M §8.4 — 현행 배포 팔이 예산의 16~27%** 만 쓴다 | **D** 🔴 **#4 에 이은 두 번째 «결정도 기전도 우리 것»** — 닫으려면 **그리퍼 포획 반경 1개** 가 필요하다(§8.3) |

### 6.3 🔴 «기전조차 우리 코드» 인 것은 **#4 하나**다

나머지는 전부 **결정만 O 이고 기전은 P/T/S** 다. **#4 만 기전도 O**여서 D 등급이다 —
그리고 §6.4 가 그 이유를 upstream 코드로 설명한다.

### 6.3a ★★★★★ **남은 미확보 목록 — 실질 하나다** (2026-09-01 감사 종료 시점)

| | 항목 | 상태 |
|---|---|---|
| 🟡 **①** | **`--select center` + `score_frac 0.9`** | 🔴 **기전 근거는 끝까지 없다**(등급 D). §6.4 가 **왜 `score` 로는 원리적으로 안 되는지**를 upstream 코드로 보인다. ★★ **그러나 «배포 조건에서 무영향» 임이 측정됐다**(§44-17) — 단일 물체 씬 **304케이스에서 `score`·`center0.9`·`center0.3` 이 0% 불일치**. → **논문에서 «근거 없는 자유 파라미터» 가 아니라 «검증 조건에서 결과를 바꾸지 않는 구성요소» 로 쓸 수 있다.** 🔴 **동종 방해물 씬에서만** 문제가 된다(오선택 15~36%, §44-15 는 `0.15~0.30` 이 최적이라 잰다). 바른 설계는 **박스 프롬프트**(§6.5) — upstream 이 지원하나 **위치 정보 필요** |
| 🟡 ② | `--text-conf 0.05` | **①에 딸린다**(§42-9 — 문턱과 선택 규칙이 얽혀 있다). ①이 배포 조건에서 무영향이므로 **이것도 그 조건에서는 검출 여부만 정한다** — 그 축은 `score_min`(§43-7·§44-5)으로 근거가 있다 |
| ⚠️ ③ | `--input-scale 0.75` | 🔴 **«못 채움» 이 결론이다** — 4회 재실행·n=120 에서 0.75↔0.5 가 **p=1.000**(§38-9). 논문에는 *"구분되지 않으므로 비용·OOM 위험으로 골랐다"* 로 쓴다 |
| ✅ ④ | `--stereo-scale 0.5` | **닫혔다**(§44-13) — 상한이 0.5625 이고, 남은 여유의 값어치가 **t 0.147mm ↔ 비용 +36%** 로 정량화됐다 |
| ✅ ⑤ | `--est-iter` / `--refine-iter` | **닫혔다**(§44-12) — `est-iter 5` 는 **저자 기본값**, `refine-iter` 는 upstream 과 어긋나지만 **2·5·10 이 잡음 바닥 안에서 구분 안 됨** |
| ⚠️ ⑥ | 정합 없음 | **측정으로만 선다**(§35-2i·§38·§43). 기전 근거는 없다 — 다만 «쓰지 않는다» 는 결정이라 기전이 필요한 종류가 아니다 |
| ⚠️ ⑦ | `--flange-mask-proj faces` | 측정(교훈 #20)뿐. **RH1 에서는 stage2 만 건드리고**, §44-2a 에서 **팔 사이 차이가 FP 잡음보다 작다**는 것이 실물에서 확인됐다 |
| 🔴 **⑧** | **KPI `t≤5mm · R≤3°`** (신규, 2026-09-02) | 🔴 **유도가 기록돼 있지 않다**(등급 D, §8) — 요구사항 문서에도 공차 유도가 없다. ★ 사후 정합성은 강하다: **3° 의 flange 표면 최대 변위 4.81mm ≈ t 예산 5mm** 라 두 항이 **flange 좌표계에서만 균형**이고, 같은 3° 가 `full` 표면에서는 22.4mm(4.5배 불균형)다. **닫으려면 «그리퍼 포획 반경» 한 숫자**가 필요하다 — 스펙시트 한 줄 또는 실측 1회 |
| 🔴 **⑨** | **배포 거리 하한의 «기전»** (강등, 2026-09-02) | 관측은 실물로 확고한데(28cm 대실패 19.4%, n=160) **기전 가설이 sim 에서 재현되지 않았다**(§44-24d). 잘림이 때린 것은 pose 가 아니라 **분할**이었다 → 등급 A → **B** |

### 6.3a-2 ★★★ **논문·보고서용 한 문단** (2026-09-02 갱신)

> *"파이프라인 구성요소 20개 중 **16개는 상위 문헌·코드 또는 표준 문서에서 기전 근거를 세웠고**,
> 둘은 **측정으로 선다**(윤곽 정합 비사용 · flange 마스크 투영 방식),
> 하나는 **관측은 확고하나 기전이 미규명**이다(배포 거리 하한).
> upstream 에 대응물이 **아예 없는** 것은 **인스턴스 선택 규칙 하나**인데, 그 한계는
> SAM3 의 점수 정의(`presence_score` 가 이미지 단위라 상대 문턱에서 상쇄되고, 남는
> `out_probs` 는 «이 개체가 그 개념인가» 에 답하므로 동종 물체를 구분하지 못함)에서
> **원리적으로 따라 나온다.** 다만 **본 연구의 평가 조건(단일 대상 장면)에서는 이 규칙이
> 결과를 바꾸지 않음을 확인했다** — 세 가지 선택 규칙이 304개 사례에서 동일한 마스크를 냈다.
> 동종 방해물이 존재하는 장면으로 확장할 때의 처방은 **위치 사전정보를 이용한 박스 프롬프트**이며,
> 이는 상위 구현이 지원한다.
> 끝으로 **합격 기준(t ≤ 5mm · R ≤ 3°) 자체는 과제 요건으로 주어진 값이고 본 연구가 유도한 것이 아니다.**
> 다만 그 쌍이 **채택한 좌표계(flange 상면 중심)에서 자기정합적**임을 보인다 — 회전 3°가 flange
> 표면을 최대 4.81mm 움직여 평행이동 예산 5mm 와 같은 크기로 기여하는 반면, 동일한 3°를 물체
> 전체 표면에서 재면 22.4mm 로 회전 항이 지배한다. 요건을 **유도**하려면 파지 기구의 포획 반경
> 한 값이 추가로 필요하며, 이는 본 연구 범위 밖이다."*

★ **이 문단이 지금 서술 가능한 최대치**다. «약점을 감추지 않으면서 그 약점이 결과에 미친 영향이
없음을 보인» 형태이고, **확장 조건과 처방까지 명시**되어 있다.

### 6.3b ★★ 반복 횟수 두 개가 **갈렸다** (2026-09-01 감사)

`--est-iter` 와 `--refine-iter` 를 «둘 다 근거 없는 5» 로 묶어 놓았던 것이 **틀렸다**:

| | 우리 값 | upstream 기본 | 어느 함수 | 판정 |
|---|---|---|---|---|
| `--est-iter` | 5 | **5** (`run_demo.py:20` · `estimater.py:159`) | `est1.register` (`pose_fp.py:374`) | ✅ **A — 저자 기본값** |
| `--refine-iter` | **5** | **2** (`run_demo.py:21` `track_refine_iter`) | `est2.track_one` (`pose_fp.py:416`) | 🔴 **B — 어긋나고 미측정** |

★ **왜 어긋났나(가설)** — upstream 의 `track_refine_iter=2` 는 **프레임 간 추적**을 상정한다
(직전 프레임 대비 자세가 거의 안 움직였으므로 2회면 족하다). 우리 stage2 는 같은 함수를 쓰지만
**메쉬를 갈아탄 직후의 «한 번뿐인» 정련**이라 초기 오차가 훨씬 크다 → 더 많은 반복이 정당할 수 있다.

✅ **측정했다 — 그 가설은 필요 없었다** (`RESULTS.md §44-12`, sim GT · 0.686m · n=16 · **설정당 3회 반복**):
**2 · 5 · 10 이 구분되지 않는다.** 짝지은 중앙차가 **R 0.000~0.035° · t 0.001~0.019mm** 인데
**같은 설정 재실행만으로 R 0.064~0.085° · t 0.071~0.093mm** 가 흔들린다 — **설정 효과가 잡음의 1/3~1/4** 다.
비용도 **1172 → 1215 → 1228 ms/frame** 으로 무시할 수준이다.

→ ★ **논문 서술**: *"stage2 의 반복 횟수는 upstream 의 추적용 기본값 2 대신 5 를 쓰되, 2·5·10 사이의
차이가 재실행 잡음 바닥 아래임을 확인했다."* 🔴 **«5 가 최적» 으로 쓰지 않는다** — 측정된 것은
«구분되지 않는다» 다. ⚠️ 이 측정은 **초기값이 좋은 구간**(0.686m, 잘림 없음)의 것이고,
28cm 처럼 초기 오차가 큰 조건에서는 다를 수 있다.

### 6.4 ★★ `score` 가 왜 타깃을 못 고르나 — **T 근거**

`third_party/sam3/sam3/model/sam3_image_processor.py:195-199`
```python
out_probs      = out_logits.sigmoid()                     # 인스턴스별 — "이 개체가 그 개념인가"
presence_score = outputs["presence_logit_dec"].sigmoid()  # 🔴 이미지별 — "이 이미지에 그 개념이 있나"
out_probs      = out_probs * presence_score               # ← 우리가 보는 score
keep = out_probs > self.confidence_threshold              # ← --confidence (절대 문턱)
```

★ **`presence_score` 는 이미지당 하나**라 한 이미지 안의 모든 후보에 **같은 값이 곱해진다.**
→ **상대 문턱(`score_frac`)에서는 완전히 상쇄**되고 **`out_probs` 의 순위만** 남는다.

🔴 그런데 `out_probs` 는 *"이 개체가 그 명사구에 맞나"* 에 답한다. FOUP 이 세 대면
**셋 다 똑같이 잘 맞는 것이 정상**이고, 진짜 타깃과 방해물의 점수 차는 **신호가 아니라 잡음**이다.
실측(§42-6): 정답 score 0.758·0.816·**0.559** ↔ 방해물 0.852·0.930·**0.965**.

★★ **«0.9×최고점» 밴드는 그 잡음 폭보다 좁다.** 튜닝으로 못 고친다 — 넓히면 잡동사니가 들어오고
좁히면 정답이 더 떨어진다. **점수는 «어느 것이 내 타깃인가» 를 원리적으로 모른다.**

### 6.4b ★★★ **그 규칙은 어디서 왔나 — 이력** (사용자 질문, 2026-09-01)

논문에 «임의로 정한 값» 으로 보이면 안 되므로 **출처를 정확히 적어 둔다.**
🔴 **둘은 «고른 값» 이 아니라 «버그를 고치다 생긴 안전장치» 다.** 정본은 `RESULTS.md §M4 ★3`.

#### ① `--select center` — **두 번 틀리고 세 번째에 맞은 것**

| 시도 | 규칙 | 결과 |
|---|---|---|
| 1차 | **중앙 근접만** | 물체의 **작은 파편**을 집었다 — ISM IoU **0.009** |
| 2차 | + 면적 필터 | 이번엔 **배경(바닥면)** 을 집었다 — precision **0.001** |
| **3차 (현행)** | **점수 상위 → 면적 필터 → 중앙 근접** | ISM IoU **0.936 · 오선택 0/40** |

★ 원인 진단은 *"점수를 버린 것"* 이었다. 그래서 `center` 는 **점수를 대체하는 규칙이 아니라
동점자 tie-break** 로 설계됐다(`contracts.py:204-207` 주석에 그대로 있다). 같은 규칙이 SAM3 텍스트에서
**오선택 18 → 1**(IoU 0.434 → 0.770)로 줄였다.
⚠️ **1차 버그 상태로 냈던 «조합 B 끝단 100%» 는 무효**였고 수정 후 순위가 바뀌었다.

#### ② `score_frac 0.9` — **«얼마가 동점인가» 의 임의 문턱. 스윕된 적이 없다**

`select_index(..., score_frac: float = 0.9)`(`contracts.py:201`)의 **기본값으로 태어나 그대로 남았다.**
2차 실패(배경 선택)를 막으려고 «최고점의 90% 이상» 을 «동점 구간» 으로 본 것이고,
**0.8·0.95 같은 대안과 비교한 기록이 없다.** → 그래서 감사에서 **등급 D**(기전도 우리 것)다.

#### ③ 왜 오래 안 들켰나 — **검증 씬에 방해물이 없었다**

- **§M2 확장**: 동일 인스턴스 씬에서 SAM3 **텍스트는 오선택 45%** 인데 `select center` 를 붙이면 **1/40**,
  ISM+center 는 **0/40**. → *"규칙이 문제를 푼다"* 로 읽혔다.
- **§M4 ★2 스트레스 테스트**: 타깃을 화면 중앙에서 **270px 밀어내도 오선택 0**. 점수가 결정적이면
  중앙 규칙은 **발동조차 안 한다**는 것이 확인됐다.
  🔴 그런데 그 절은 스스로 한계를 적어 뒀다 — *"주점(cx) 이동으로 만든 오프센터라 **ISM 점수가 선호하는
  대상은 바뀌지 않는다**. «중앙 규칙이 필요한 상황» 을 만들어내지 못했다."*
- ★★ **그 «필요한 상황» 이 §42 에서 처음 만들어졌다** — **방해물 FOUP 이 있는 씬 + GT**.
  거기서 **분할은 맞았는데**(정답 후보의 IoU 0.799/0.966/0.959, 항상 최대 면적)
  **`score_frac 0.9` 게이트가 정답을 버리고 있었다**(§42-6). **영향 KPI 22%p.**
- 🔴 그리고 **문턱과 짝이다** — 게이트를 끄면 `conf 0.15` 에서는 오선택 0% 인데 **`conf 0.05` 에서는 76.7%** 다(§42-9).
  **한쪽만 바꾸면 안 된다.**

#### ④ → 논문에 쓸 서술

*"인스턴스 선택은 점수 상위 집합(최고점의 90% 이상) → 면적 필터 → 화면 중심 근접의 3단 규칙이다.
이는 상위 모델이 제공하지 않는 부분으로, 단일 인스턴스 장면에서 파편·배경 선택을 막기 위해 도입됐고
그 조건에서는 오선택 0/40 이었다. **동종 방해물이 있는 장면에서는 이 규칙이 실패한다**(§6.4) —
SAM3 의 점수는 «이 개체가 그 개념인가» 에 답하므로 동종 물체를 구분하지 못하며, 90% 문턱은
정답을 배제할 수 있다. 원리적 해법은 위치 사전정보를 이용한 박스 프롬프트다(§6.5)."*
★ **«튜닝했는데 부족했다» 가 아니라 «설계 가정이 장면 종류에 묶여 있었다» 가 정확한 서술**이다.

### 6.5 ★★★ 바른 설계도 upstream 에 있다 — **박스 프롬프트** (P/T)

- **P** `sam3/README:49` — *"using text or **visual prompts such as points, boxes, and masks**"*
- **T** `sam3_image_processor.py:129` `add_geometric_prompt(box, label, state)` —
  `[center_x, center_y, width, height]` 정규화 좌표로 **«이것» 이라고 직접 가리킨다.**

→ 타깃 지정을 **점수(개념 유사도)가 아니라 기하 프롬프트**로 옮기면 #4 가 **D → A** 가 된다.

🔴 **§38-1 에서 실패한 것과 혼동하지 말 것** — 그건 **«미리 만든 참조 이미지 세트»** 이고
이건 **«이 프레임의 좌표»** 다. 참조 세트는 도메인 갭에 노출되지만 **좌표는 그렇지 않다.**

⚠️⚠️ **그런데 지금은 못 쓴다** — 박스를 만들려면 **대략적 위치**가 필요하고(로봇 좌표·티칭값),
현 환경엔 로봇이 없다. **그래서 RH1 은 바뀌지 않는다.** 이것은 **방해물이 있는 배치에 대한
조건부 처방**으로만 기록한다. 근거는 §34-10(성공군 4.4mm ↔ 실패군 447mm, **102배 간격**).

---

## 7. 인용할 코드 위치 (원문 확인 완료)

| 주장 | 위치 |
|---|---|
| R·t 갱신이 분리된다 | `Utils.py:850-857` `egocentric_delta_pose_to_pose` |
| crop 이 3D 에서 `diameter × crop_ratio` | `Utils.py:605` · `weights/*/config.yml`(`crop_ratio` 1.2/1.1, `input_resize` 160) |
| 평행이동 보폭만 메쉬 크기에 비례 | `predict_pose_refine.py:221`(`rot_normalizer` 0.3490658…=20°) · `:229`(`trans_delta *= mesh_diameter/2`) |
| stage2 가 메쉬를 갈아탄다 | `spatial_vision/stages/pose_fp.py:286-293` |
| 마스크는 초기화에만 쓰인다 | `estimater.py:137-150` `guess_translation`(**bbox 중심 + depth 중앙값**) · `:184-206`(`rgb`/`depth` 는 **마스킹되지 않는다**) |
| 하이브리드 구현 | `spatial_vision/eval/hybrid_pose.py` (`--r-dir`/`--t-dir`) |

---

## 8. ★★ KPI 「t ≤ 5mm · R ≤ 3°」 선정 근거 — **유도는 없고, 사후 정합성은 강하다** (사용자 질문, 2026-09-02)

### 8.1 🔴 문서 감사 — 유도가 기록돼 있지 않다

- `RESULTS.md §KPI` : *"2026-08-07 확정"* + 표. **왜 5mm 이고 왜 3° 인지가 없다.**
- `CONSUMER_6DPOSE.md`(= 「요구사항 근거」 담당 문서) : **공차 유도가 없다.** 있는 것은 선행연구 수치뿐이다 —
  FoundationPose YCB-V `2.5mm / 5.8°`(표1) · 그 연구의 실물 결과 `t 평균 0.95mm / 최대 2.30mm`.
- ⚠️ **「선행연구 최대 2.30mm 의 약 2배」 라는 유래는 그럴듯하지만 기록이 없다** — 추정으로만 적는다.

→ 감사표 **#20 = D 등급**. `#4`(선택 규칙)에 이은 **두 번째 «결정도 기전도 우리 것»** 이다.

### 8.2 ★★★ 사후 정합성 — **두 숫자는 flange 좌표계에서만 균형이 맞는다**

회전 오차는 **길이가 아니다.** 물리적으로 의미가 생기려면 *"어느 점이 얼마나 움직이나"* 로 환산해야 하고,
그 값은 **원점에서의 거리 `r`** 에 비례한다: `d = 2·r·sin(θ/2)`.
현행 원점은 **flange 주 상면 중심**이다(`meta.json`, 감사표 #12).

| θ = 3° 일 때 표면이 움직이는 양 | 원점 거리 `r` 최대 | 중앙 | p90 | **최대** |
|---|---|---|---|---|
| **`top_flange.ply`** | 91.8mm | 2.82mm | 4.18mm | **4.81mm** |
| `full.ply` | 427.3mm | 3.22mm | 15.57mm | **22.37mm** |

★★★ **`3°` 의 flange 표면 최대 변위 4.81mm 가 `t` 예산 5mm 와 거의 같다.**
즉 두 항이 **같은 크기로 기여**한다 — 한쪽이 다른 쪽을 지배하지 않는 균형점이다.
🔴 **같은 3° 를 `full` 표면에서 재면 22.4mm 로 t 예산의 4.5배**가 되어 회전이 완전히 지배한다.

→ ★ **결론: 「t≤5mm · R≤3°」 라는 쌍은 «flange 를 잡는 작업» 에 맞춰진 값이다.**
   원점을 몸체로 옮기면 그 쌍은 더 이상 균형이 아니다. **KPI 와 원점 규약은 분리해서 인용할 수 없다.**
   ⚠️ 이것은 **사후 정합성**이지 유도가 아니다 — 설계자가 그렇게 의도했다는 기록은 없다.

### 8.3 🔴 비어 있는 한 숫자 — **그리퍼 포획 반경**

우리가 가진 참조 스케일:

| 출처 | 값 | 시사 |
|---|---|---|
| **S** SEMI E47.1 flange 자체 공차 | 노치 위치 **±1mm** · `x46 71±1` · `d63 ø35±0.1` · `θ 45±0.5°` | **KPI 5mm 는 부품 제조 공차의 5배**다 → **부품 규격에서 유도된 값일 수 없다** |
| 선행연구 실물 | `t 평균 0.95mm / 최대 2.30mm` | 달성 가능 대역 |
| 우리 실물 | `full` 경로 t 중앙 **2.20~2.87mm** | 예산 안 |

★ **KPI 를 «유도» 하려면 로봇 쪽 숫자가 하나 필요하다** — 로봇 핸드가 flange 에 결합할 때
**리드인 챔퍼·수동 컴플라이언스가 흡수하는 최대 오정렬** `c`. 그러면 요건이 그대로 나온다:

```
t_err  +  r_max · 2·sin(R_err / 2)  ≤  c        (r_max = 91.8mm, flange 최외곽)
```

현행 `(5mm, 3°)` 는 좌변 최악값이 **9.81mm** 이므로 **`c ≈ 10mm` 를 암묵 가정한 것과 같다.**
→ 🔴 **스펙시트 한 줄 또는 실측 1회로 닫히는 항목**이다. 로봇이 정해지면 가장 먼저 받을 숫자다.
⚠️ 그 전까지 *"KPI 를 근거로 구성 A 가 배포 가능하다"* 라고 쓰면 안 된다 — **문턱 자체가 미확정**이다.

### 8.4 ★★ 현행 배포 팔의 예산 소진 — **16~27%**

좌표계 무관 지표(**`top_flange` 표면의 최대 변위**)로 환산한 값. 씬 고정 사다리 · `RH1` · `score_frac 0.3`:

| 거리(m) | n | t 중앙 | R 중앙 | **flange 표면 변위 중앙** | p90 | **예산(9.81mm) 소진** |
|---|:-:|---|---|---|---|---|
| 0.291 | 79 | 0.708mm | 0.636° | **1.585mm** | 2.640 | **16%** |
| 0.491 | 79 | 1.292mm | 0.485° | **1.876mm** | 2.478 | **19%** |
| 0.597 | 79 | 1.777mm | 0.484° | **2.292mm** | 3.212 | **23%** |
| 0.850 | 78 | 2.214mm | 0.467° | **2.629mm** | 3.819 | **27%** |

⚠️ 0.597m 의 **최대가 420mm** 인 프레임이 하나 있다 — 마스크는 IoU ≥0.3 을 통과했는데 pose 가 날아간
경우다(그 거리의 78/80 중 하나). **중앙값만 보면 안 되는 이유**(교훈 #14)의 실례로 같이 인용할 것.

### 8.5 ★ 권고 — **보고 형식에 좌표계 무관 지표를 하나 더 붙인다**

`(t, R)` 쌍은 **좌표계 의존**이라 그 자체로는 논문 간 비교가 불가능하다(§8.2 가 그 증거다).
그래서 논문·보고서에는 **셋을 같이** 낸다:

1. **좌표계 명시** — *"pose frame origin = flange dominant top plane center"*
2. `(t, R)` — 기존 KPI
3. ★ **`top_flange` 표면 최대 변위**(= flange 메쉬에 대한 ADD-max) — **HW 인터페이스가 실제로 겪는 양**이고
   좌표계 선택에 불변이다. 위 표가 그 값이다.

🔴 **이 절은 유도가 아니라 «정합성 + 빈칸의 위치» 다.** 논문에는 그대로 그렇게 쓴다 —
*"KPI 는 과제 요건으로 주어졌고, 우리는 그것이 flange 좌표계에서 자기정합적임을 보이고,
유도에 필요한 유일한 미확보 값(그리퍼 포획 반경)을 명시한다."*
